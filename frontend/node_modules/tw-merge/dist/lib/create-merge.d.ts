import { RuleSet } from "../rules";
export type CreateMergeConfig = {
    cacheSize?: number;
    separator?: string;
    prefix?: string;
};
export declare function createMerge(ruleSet: RuleSet, { cacheSize, separator, prefix }?: CreateMergeConfig): (className: string) => string;
